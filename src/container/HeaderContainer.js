import React from "react";
import styled from "styled-components";

const Header = styled.header`
  background-color: #706fd3;
`;

const HeaderContainer = () => {
  return <Header>Hello React Header!!</Header>;
};

export default HeaderContainer;
