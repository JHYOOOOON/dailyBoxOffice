import React from "react";
import ReactLoading from "react-loading";

const Loading = () => (
  <div className="loading">
    <ReactLoading type="bubbles" color="#2c3742" width={150} margin="0 auto" />
  </div>
);

export default Loading;
